## 1. CSS

- [x] 1.1 Add `.role-toggle` component styles to `site.css` (toggle group container, toggle buttons, active state using `--accent`)

## 2. Join Page

- [x] 2.1 Replace the `<select asp-for="Role">` on `Join.cshtml` with a hidden input + two toggle buttons, wired with `onclick` to update the hidden value and active class

## 3. Manage Page

- [x] 3.1 Replace the `<select asp-for="Role">` on `Manage.cshtml` "Add dancer" form with the same hidden input + toggle button pattern

## 4. Verify

- [x] 4.1 Build the project and confirm no compilation errors
