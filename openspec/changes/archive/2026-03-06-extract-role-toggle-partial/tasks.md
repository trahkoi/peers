## 1. Extract Partial

- [x] 1.1 Create `Pages/Sessions/_RoleToggle.cshtml` containing the role toggle markup (label, hidden input, toggle div with buttons)

## 2. Replace Inline Markup

- [x] 2.1 Replace the role toggle markup in `Join.cshtml` with `<partial name="_RoleToggle" />`
- [x] 2.2 Replace the role toggle markup in `Manage.cshtml` with `<partial name="_RoleToggle" />`

## 3. Verify

- [x] 3.1 Build the project and confirm no compilation errors
